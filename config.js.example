const Pool = require('pg').Pool
const pool = new Pool({
    user: 'username',
    host: 'localhost',
    database: 'database',
    password: 'password',
    port: port number,
});

module.exports = pool